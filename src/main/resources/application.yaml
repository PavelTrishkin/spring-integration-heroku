server:
  hostname: localhost
  port: ${PORT:8090}
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DATABASE_PATH_INTEGRATE_MODULE}
    username: ${DATABASE_USER_INTEGRATE_MODULE}
    password: ${DATABASE_PASSWORD_INTEGRATE_MODULE}
